# Any property can be overwritten from command-line via
#   -P<propertyName>=<value>

DotnetPluginId=ReSharperPlugin.InspectionCopyBackend
DotnetSolution=ReSharperPlugin.InspectionCopyBackend.sln
RiderPluginId=com.codex.rider.inspectioncopy
PluginVersion=0.8.0

BuildConfiguration=Debug

PublishToken="_PLACEHOLDER_"

# See https://www.jetbrains.com/intellij-repository/snapshots
# Keep in sync with SdkVersion in Plugin.props -->
#
# Examples:
#   Release:  2020.2
#   EAP:      2020.3-EAP2-SNAPSHOT
#   Nightly:  2020.3-SNAPSHOT
ProductVersion=2026.2

# Keep the Kotlin standard library dependency aligned with the version bundled with the IDE.
kotlin.stdlib.default.dependency=false

# Required to download Rider artifacts from Maven (and not "binary" releases from CDN).
org.jetbrains.intellij.platform.buildFeature.useBinaryReleases=false

# Reuse Gradle's configuration model between invocations when task inputs are unchanged.
org.gradle.configuration-cache=true
